# Allow Lombok-generated constructors (@RequiredArgsConstructor) to copy Spring's
# @Qualifier onto constructor parameters, so qualified bean injection works on
# final fields (e.g. split LLM provider routing in DF-7).
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
config.stopBubbling = true
